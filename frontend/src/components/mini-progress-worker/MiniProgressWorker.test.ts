// Node modules
import { expect, test } from "vitest";

// Project files
import { render, screen } from "scripts/testing-library/assignments-page-globals";

test.todo("Receives an array of 1 good candidates and reports 1 normal scan");

test.todo(
  "Receives an array of 1 candidates with only a few fields missing and reports 1 normal scan",
);

test.todo("Receives an array of 3 good candidates and reports 3 normal scans");

test.todo("Receives an array with 1 profile of each type and report 4 scans");

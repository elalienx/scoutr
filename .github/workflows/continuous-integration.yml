name: Scoutr Test Suite

on:
  pull_request:
    branches:
      - main
jobs:
  backend-test:
    name: Backend unit testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t docker-backend-test ./backend

      - name: Run Vitest inside Docker container
        run: docker run docker-backend-test npm run test_once

  frontend-test:
    name: Frontend unit testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t docker-frontend-test ./frontend

      - name: Run Vitest inside Docker container
        run: docker run docker-frontend-test npm run test_once
